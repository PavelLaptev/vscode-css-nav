{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (GitButler)",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "/Users/pavellaptev/Documents/GitHub/gitbutler"
      ],
      "outFiles": ["${workspaceFolder}/out/**/*.js"],
      "preLaunchTask": "${defaultBuildTask}"
    }
  ]
}
